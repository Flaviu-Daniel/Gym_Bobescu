@page
@model Gym_Bobescu.Pages.Sali.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Sali</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Sala[0].NumeSala)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sala[0].Oras)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sala[0].Strada)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Sala) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumeSala)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Oras)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Strada)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
